# 出海指挥官 Export Director — 技术说明

> AI × 跨境黑客松挑战赛 · 场景二（AI 智能营销）｜ 复赛
> 提交物：可运行 Demo / 代码仓库 / 演示视频 / 技术说明 / 体验方式

## 一、一句话定位

**让跨境营销从“人肉跑工具”变成“对话一位 AI 大管家”。**

用户只需一句对话，系统自动完成：**商品策略 → 全平台素材 → 红人种草/智能投流 → 投放诊断 → 优化迭代** 的完整闭环。
第三方成熟的营销工具负责“做”，大管家负责“想、指挥、解读、复盘、进化”。

## 二、核心创意：大管家（本项目灵魂）

### 2.1 为什么需要大管家

市面上的选品、设计、红人、投放、归因工具已经不少，但彼此割裂：每个都很强，数据却不互通，决策靠人肉搬运。
本项目**不造第 N+1 个单点轮子**，而是造一个“中间智能层”——把第三方成熟能力**组织起来、编排起来、串联起来**。

### 2.2 大管家三层智能

- **Plan / 计划**：听懂一句话（“全流程”“只跑投流”“这次主推红人”），自动拆解任务、编排顺序与上下文；
- **Analyze / 分析**：每个模块返回结果后，立即提炼「关键发现 / 风险 / 下一步动作」，而不是把数据原样罗列；
- **Optimize / 优化**：全流程结束，综合全部结果，给出优化建议并**自动回流到下一轮起点**，实现自我迭代。

### 2.3 为什么是“1 + 1 > 2”

模块之间互不感知，但大管家让它们**接力协作、数据互通**：

> 策略输出的渠道/卖点 → 喂给素材与投放 → 投放数据交给诊断归因 → 诊断结论沉淀为下一轮策略约束。

单点工具的能力是“点”，大管家把“点”连成“飞轮”：**越做越准、越跑越好**。这是任何单一工具都给不了的。

### 2.4 架构取舍：解耦

- **大管家 = 大脑（编排 / 分析 / 优化）**：与业务模块零耦合；
- **业务模块 = 手脚（服务）**：每个都是“笨服务”——输入输出，不关心其他模块；
- 未来更换任何一家第三方工具，只需替换对应模块的数据源，**管家层与关闭流程都不用动**。

## 三、系统如何运转（闭环示意）

```
用户的一句话（或一键指令）
        │
        ▼
   🧠 大管家（Plan → Analyze → Optimize）
        │ 编排并接力传递上下文
   ┌────┼────┬────┬────┐
   ▼    ▼     ▼    ▼    ▼
 ①策略 ②素材 ③达人 ④投流  ⑤诊断
 选品/  文案/  种草/  智能   ROI/归因/
 渠道/  主图/  触达    投放   迭代
 预算   视频
        │
        ▼
   每步结果回传管家分析 → 全流程优化复盘 → 反哺下一轮（闭环）
```

- **双引擎**：达人种草 ⊕ 智能投流，可独立、可并行、可组合（“种草 + 放大”双轮驱动）。
- **多商品库**：一个卖家管理多个商品，各自独立策略 / 素材 / 投放 / 多轮历史/效果。
- **ROUND 效果卡**：每轮 ROAS / 花费 / 收入横向对比，让“一轮比一轮好”肉眼可见。

## 四、技术实现（要点）

| 项 | 实现 |
|---|---|
| 形态 | Streamlit 三栏指挥台（左=商品库与参数，中=第三方模块矩阵，右=大管家对话，顶部=闭环流水线与 ROUND 卡） |
| 栈 | Python + OpenAI 兼容 SDK；文本推理 / 出图：通义系模型 |
| 双通道 | 内置演示数据引擎（离线、零成本、默认）+ 真实大模型接入（一键切换） |
| 工程 | .env 配置，MOCK_MODE 开关，模块统一“输入→输出”签名，替换数据源不动管家层 |

## 五、运行与体验

- 演示视频：`演示视频/demo.mp4`（全流程并附中文旁白解说）
- 线上 Demo：https://export-commander-ai-6yxng2upjur3gqkdccmbfq.streamlit.app/
- 本地运行：`pip install -r requirements.txt` → `streamlit run frontend/app.py`

**三步体验**：① 打开页面 → ② 对右侧大管家说“全流程” → ③ 观察模块逐一亮起+管家逐段解读，再次运行看迭代反哺。

## 六、路线图（可延展空间）

- **近期（决赛/上线）**：接入真实第三方 API（投放后台、归因平台、达人平台），替换模拟数据源；多账号数据隔离。
- **二期**：引入实时市场信号（搜索热度、竞品价格、节令趋势）驱动策略；大管家从“给建议”升级为“半自动执行”（调预算/暂低效广告组，人工一键确认）。
- **三期**：多店铺多语言一屏归，大管家成为“跨境自动运营座舱”——规划、执行、监控、复盘、进化全自动，卖家只做“选品与决策”，把苦活用交给 AI。

## 附注

本项目已适配阿里云百炼大模型调用（OpenAI 兼容协议），代码内置真实接入入口；当前演示受 Token 额度限制，使用内置演示数据引擎，离线可跑、不消耗额度，配置真实 Key 后一键切换。
