# 出海指挥官（Export Commander）技术说明

> 竞赛：AI+跨境黑客松巅峰赛 · 复赛
> 场景：场景二 · AI 智能营销
> 提交物：可运行 Demo / 代码仓库 / 演示视频 / 技术说明 / 体验方式

---

## 一、项目概览

**出海指挥官**是一套“**大管家×第三方服务矩阵**”的跨境电商 AI 营销闭环系统：
用户只需一句对话（或点一个按钮），系统即自动完成
**营销策略 → 全平台素材生成 → 红人种草/多渠道投流 → 投放诊断 → 迭代优化 → 反哺下一轮** 的完整闭环，
并在每一步由“大管家”分析结果、给出可执行的优化建议，实现**可自我迭代的增长引擎**。

- 定位：面向跨境电商卖家/品牌的 **AI 营销操盘手（Growth Steward）**
- 形态：Web 应用（Streamlit），支持一屏三栏可视化指挥台
- 关键差异点：**不是又一个"单点工具"**，而是把第三方成熟能力（选品/设计/红人/投放/归因）**串联、编排、分析、闭环**的中间智能层。

---

## 二、解决的问题与核心创新

### 2.1 行业痛点
| 痛点 | 现状 |
|---|---|
| 链路割裂 | 内容创作、红人营销、广告投放、归因分析各用一套工具，数据互相不通 |
| 门槛高 | 新手卖家不会定策略、不会写多平台文案、不会投放、不会看 ROI |
| 决策靠拍脑袋 | 复盘靠猜，缺少“数据→结论→动作→再验证”的闭环 |
| 工具碎片 | 每换一个工具要学一次界面，重复劳动严重 |

### 2.2 我们的解法 & 创新点
1. **管家智能层（大脑）**：`策略(Plan) → 分析(Analyze) → 优化(Optimize)` 三层智能，与业务模块在架构上**彻底剥离**——管家负责调度、解读、复盘；模块负责执行。这是本项目的**架构核心亮点**。
2. **模块服务化（手脚）**：5 个业务模块各自对接成熟第三方能力（同类工具罗列），模块内可**切换 / 多选 / 独立运行**，管家在调用间负责信息传递，模块之间不互相感知。
3. **双引擎并行**：`红人种草 ⊕ 投流放大` 是两条可独立、可并行的增长路径，明确体现“种草放大”与“付费放大”的关系。
4. **闭环自动反哺**：每一轮（ROUND）结束生成迭代建议，下一轮执行时**自动应用到策略生成**，实现自我进化。
5. **商品库支撑批量运营**：一个卖家可管理多个商品，每个商品独立维护自己的策略/素材/红人/投放/诊断历史与多轮效果数据。
6. **零成本可演示**：提供完整**模拟数据引擎**（离线、不消耗任何 Token），现场随时可跑完整闭环；在有额度时一键切回真实大模型。

---

## 三、系统架构

```
                        ┌──────────────────────────────────────────┐
                        │           用户：对话 / 快捷指令             │
                        └──────────────────┬───────────────────────┘
                                           ▼
                        ┌──────────────────────────────────────────┐
                        │  🧠 大管家（大脑 · 智能编排层）              │
                        │   ● 意图理解+任务规划 (Plan)               │
                        │   ● 执行队列调度 (exec_queue)              │
                        │   ● 单模块结果分析 (Analyze)               │
                        │   ● 全流程综合优化 (Optimize)              │
                        │   ● 状态摘要 / 会话管理                     │
                        └──────────┬───────┬───────┬───────────────┘
                                   ▼       ▼       ▼
        ┌──────────────┐ ┌──────────────┐ ┌─────────────────┐ ┌──────────────┐ ┌──────────────┐
        │ 1.营销策略    │ │ 2.AI素材工厂 │ │ 3.红人种草       │ │4.投流放大      │ │5.诊断与迭代   │
        │ 卖家精灵      │ │ Canva        │ │ Upfluence       │ │TikTok Ads     │ │ Northbeam    │
        │ SimilarWeb    │ │ AdCreative.ai│ │ Grin            │ │Meta Ads       │ │ Triple Whale │
        │ Jungle Scout  │ │ Midjourney   │ │ HypeAuditor     │ │Google Ads     │ │ Rockerbox    │
        │              │ │ Synthesia    │ │ (可与4并行)      │ │(可与3并行)     │ │              │
        └──────────────┘ └──────────────┘ └─────────────────┘ └──────────────┘ └──────────────┘
                                     ▼ 结果全部回传大管家进行分析/优化
        ┌──────────────────────────────────────────────────────────────────────────────┐
        │ 数据层：模拟演示引擎(MOCK_MODE) / 阿里云百炼 Token Plan(OpenAI兼容)              │
        │ 协议层：Qwen 系列文本推理 + 通义万相出图；OpenAI SDK 兼容调用                    │
        └──────────────────────────────────────────────────────────────────────────────┘
```

### 分层说明
| 层 | 组成 | 职责 |
|---|---|---|
| 展示层 | `frontend/app.py`（Streamlit 三栏） | 左=商品库与参数；中=五大模块矩阵（第三方 Logo/状态）；右=大管家对话；顶部=闭环流水线与 ROUND 效果卡 |
| 编排层 | `steward_chat / steward_analyze / steward_optimize` + 队列执行 | 意图识别、任务计划、逐模块执行→分析→展示、全流程优化 |
| 服务层 | `generate_strategy / generate_content / generate_kol / diagnose_ads / generate_loop` | 每个模块是“笨服务”：输入上下文→输出结构化结果，不感知其他模块 |
| 数据层 | `app/llm.py`（真实）/ `app/mockdata.py`（模拟） | 统一接口，模式切换只改配置 |

---

## 四、五大模块设计（第三方能力对接）

| 模块 | 职责示例 | 画像中的第三方/竞品 | 关键输出 |
|---|---|---|---|
| 1 营销策略 | 市场定位、渠道选择、预算分配、内容节奏 | SimilarWeb / Jungle Scout / 卖家精灵 | 市场定位、目标受众、渠道策略(3渠道)、预算分配饼、KOL建议 |
| 2 AI素材工厂 | 多平台文案+产品主图(离线SVG/真实出图) | Canva / AdCreative.ai / Midjourney / Synthesia | TikTok/IG/Meta 三端文案、核心卖点、本地化提示、主图 |
| 3 红人种草 | 达人匹配+触达话术 | Upfluence / Grin / HypeAuditor | 4 位匹配（平台/粉丝/互动率/匹配度/报价）、外联话术 |
| 4 投流放大 | 多渠道广告计划+数据模拟 | TikTok Ads / Meta Ads / Google Ads | 广告组级 CSV（广告组/花费/转化/ROAS） |
| 5 诊断与迭代 | ROI 体检、浪费预算、动作清单、迭代建议 | Northbeam / Triple Whale / Rockerbox | 整体评估、广告组诊断、动作清单、预算重分配、创意疲劳 |
| 横 | 大管家分析 | 每次模块执行后给出 2-3 条关键发现+风险+下一步 | ≈60 字中文洞察 |

> 演示版以“模拟数据”在模块内复现各家能力；真实接入时，模块函数签名不变，仅将数据源替换为对应平台 API（见 8.3）。

---

## 五、管家智能层（架构核心）

```
输入(用户对话/商品状态)
   │
   ▼
[路由]  keyword → 动作 (全流程/单模块/投放/红人/诊断)
   │
   ▼
[执行] 逐步骤 exec_queue（睡1s 轮询渲染, 页面实时更新）
   │       每个模块执行完
   ▼
[Analyze] 对"该模块结果摘要"调用LLM → 得出 关键发现/风险/下一步
   │
   ▼
[Optimize] 全流程结束后综合 5 模块结果 → 整体评价+3条优化+下轮重点
   │
   ▼
  写回 loop 反馈 → 下一轮起点自动应用（闭环）
```

- 分析/优化使用快速模型（`qwen3.6-flash`，响应快），主流程用主模型（`qwen3.7-plus`/`qwen3.8-max`），在“质量”与“等待”间做取舍。
- 全流程总步数 8（规划→5模块→优化复盘），每一步在界面实时展示（流水线节点点亮、ROUND 效果卡）。

---

## 六、闭环与数据流

一次“全流程”：

```
🧭策略 → 🎨素材 → [🎯红人种草 ⊕ 📣多渠投流] → 🔍诊断 → 🔄迭代 → 🔁反哺下一轮
```

- 每商品独立：策略/素材/红人/投放/诊断/迭代 均有历史记录，可回看。
- 顶部 ROUND 卡存储每一轮：ROAS、花费、收入、服务组合 → 可视化“越跑越好”。
- 迭代建议（内容pivot、新卖点、风格、渠道、预算）在下一轮策略生成时**自动注入提示**。

---

## 七、技术栈

| 层 | 技术 |
|---|---|
| 前端 | Streamlit（暗色科技风三栏，原生组件自绘） |
| 后端 | Python 3.11 + OpenAI SDK（百炼 OpenAI 兼容协议） |
| 模型 | 文本：qwen3.8-max / qwen3.6-flash（管家） ；出图：wan2.7-image-pro |
| 数据 | pandas 读取广告 CSV、JSON 结构化 |
| 部署 | Streamlit Community Cloud / Docker / 本地 |
| 工程 | .env 环境变量、MOCK_MODE 开关、hub 脚本级自测 |

---

## 八、模拟与真实模式

| | 模拟模式（默认） | 真实模式 |
|---|---|---|
| 触发 | `MOCK_MODE=true` | `MOCK_MODE=false` + .env 填好 API_KEY |
| 成本 | 0（全程离线） | 按 Token/图表计费 |
| 数据 | 内置 `mockdata.py` 生成全链路可复现数据（包括 CSV 广告数据、SVG 主图） | 真实大模型实时输出 |
| 适合 | 比赛演示/教学/无额度环境 | 生产运营 |
| 切换 | 侧边栏隐藏开关（演示不暴露） | 同一开关 |

> 为不影响演示效果，界面上不展示“模拟”字样；开关保留在左侧边栏（默认收起），便于实测切换。

---

## 九、体验方式（已部署）

| 方式 | 地址/命令 |
|---|---|
| 线上 Demo（推荐） | https://export-commander-ai-6yxng2upjur3gqkdccmbfq.streamlit.app/ |
| 本地运行 | `pip install -r requirements.txt`` && `streamlit run frontend/app.py`` |

**3 步体验**
1. 打开线上链接，右侧大管家输入 **“全流程”**（或点击快捷指令 🚀 全流程）；
2. 观察 8 个步骤自动执行：策略 → 素材（文案+主图）→ 红人 → 多渠道投放 → 诊断 → 迭代 → 优化建议；
3. 顶部出现 **ROUND 1** 效果卡（ROAS/花费/收入）→ 再次输入“全流程”看迭代建议自动反哺。

---

## 十、代码结构

```
.
├── frontend/app.py          # Streamlit 三栏 UI + 队列执行
├── app/
│   ├── config.py            # 环境变量 + MOCK_MODE
│   ├── llm.py               # 百炼 OpenAI 兼容客户端（懒加载）
│   ├── prompts.py           # 模块+管家 Prompt
│   ├── mockdata.py          # 全链路模拟引擎（离线演示）
│   ├── agents/core.py       # 5 模块函数 + 管家函数
│   └── tools/mock_data.py   # 广告 CSV 模拟
├── README.md                # 仓库简介+体验方式
├── 技术说明.md               # 本文档
├── 交接文档.md               # 内部开发文档
├── Dockerfile / runtime.txt/ 托管配置
└── 演示视频/ demo.mp4    # 全流程演示（提交）
```

---

## 十一、后续规划（若进入决赛/投产）

1. 真实接入：将模拟数据引擎逐步替换为各平台真实 API（投放平台、归因平台、红人平台），保持“管家层不变”；
2. 多账号/多店铺：增加账户体系、数据隔离；
3. 策略库：沉淀高频玩法（爆品打法、节日营销等）为可复用策略模板；
4. 效果报表：支持导出/周报 / 邮件订阅；
5. 红人自动化触达：接入邮件/IM 自动触达链路。