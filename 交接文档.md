# 出海指挥官 (Export Commander) — 交接文档

## 项目概述
- **竞赛**: 天池「AI+跨境黑客松巅峰赛 #532509」场景二 · AI智能营销
- **定位**: 跨境出海 AI 营销闭环系统 — 大管家(大脑) + 第三方服务(手脚)
- **技术栈**: Streamlit + 阿里云百炼 Token Plan API (OpenAI 兼容协议)
- **入口**: `frontend/app.py`，本地运行

## 核心架构
**管家=大脑(策略→分析→优化), 模块=手脚(独立第三方服务)**

全流程闭环（5 模块）：
`策略 → 素材 → [红人种草 ⊕ 投流放大] → 诊断 → 迭代 → 反哺策略`

- 模块 1 营销策略: 卖家精灵 / SimilarWeb / Jungle Scout
- 模块 2 AI素材工厂: Canva / AdCreative.ai / Midjourney / Synthesia（多选）
- 模块 3 红人种草: Upfluence / Grin / HypeAuditor（可独立，不需先投流）
- 模块 4 投流放大: TikTok Ads / Meta Ads / Google Ads（多选，默认全选，可独立）
- 模块 5 诊断迭代: Northbeam / Triple Whale / Rockerbox
- 右栏大管家: 对话 + 每步分析(📊) + 全流程优化(🎯)，自动切换/串联模块

## 代码结构
- `frontend/app.py` — Streamlit 三栏 UI + 队列执行 (`exec_queue` 每 rerun 执行一步)
- `app/agents/core.py` — 模块函数 + 管家函数 (generate_strategy/content/kol/diagnose_ads/generate_loop + steward_chat/analyze/optimize)
- `app/prompts.py` — 全部 prompt（模块输出 JSON，管家输出中文分析）
- `app/llm.py` — API 调用（429 退避重试，`qwen3.7-plus` 主模型 + `qwen3.6-flash` 管家快模型 + `wan2.7-image-pro` 出图）
- `app/tools/mock_data.py` — 模拟投放 CSV（动态广告组名，多平台）
- `.env` — API Key + Base URL（**勿外传**）
- `.streamlit/config.toml` — 暗色科技主题

## 如何运行
```bash
cd D:\AI项目\阿里云百炼
streamlit run frontend/app.py --server.port 8501 --server.headless true
```
- 语法检查: `python -c "import py_compile; py_compile.compile('frontend/app.py', doraise=True)"`

## 数据库/会话
- 全部在 `st.session_state` 内存中：12 个商品库、执行队列、对话消息、循环历史(cycle_history)
- 每商品独立保存 4模块结果 + 历史 + 服务选择

## 🚨 已知问题（重要）
- **`exceeded retry limit, last status: 429`** = **Token Plan 套餐额度已用完**，不是并发限流！
  - 不要试图用限流/退避解决，加等候无意义。
  - 解决办法: 充值/续费 Token Plan 额度后自动恢复。
- 全流程一次约 10+ 次 API 调用，额度消耗快，demo 前确认额度充足。
- Streamlit 重启会清空会话状态，演示前先跑完整流程生成记录。

## 下一步建议
- 演示选一个商品（如 p1 便携蓝牙音箱 `全流程` 一键闭环，截取各模块输出 + 管家分析 + 循环历史
- 可加: 导出报告 PDF/PPT、多商品对比看板、英文版演示素材
